# Copy to .env or .env.local. Used by SvelteKit server ($env/dynamic/private).

# --- Local frontend + real backend on Cloudflare ---
# Point to your deployed Worker so local dev talks to production API/D1.
WORKER_URL=https://hodl-or-fold-api.workers.dev
API_SECRET=your-production-api-secret

# --- Local frontend + local backend ---
# Use when running wrangler dev in packages/backend.
# WORKER_URL=http://127.0.0.1:8787
# API_SECRET=your-local-dev-secret
